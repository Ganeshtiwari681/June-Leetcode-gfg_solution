class Solution {
public:
    int scoreOfString(string s) {
        int sum=0;
        int a1=0,b1=0;
        for(int i=0;i<s.length()-1;i++){
            sum=sum+abs(s[i]-s[i+1]);
        }
        return sum;
    }
};
